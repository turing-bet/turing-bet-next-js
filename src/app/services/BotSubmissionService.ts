import { BotSubmission } from "../model/botSubmission";
import { Submission } from "../model/submission";
class BotSubmissionService {
    async setupBotSubmission(contextSubmissions: Submission[], ): Promise<BotSubmission> {
    }


    async shuffleBotSubmission(submissions: Submission[], botSubmission): Promise<BotSubmission[]> {
    }
}
